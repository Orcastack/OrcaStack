apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: orcastack-barbican
spec:
  provider: barbican
  parameters:
    roleName: orcastack-operator
    secretID: replace-with-barbican-secret-id
  secretObjects:
    - secretName: orcastack-runtime-secrets
      type: Opaque
      data:
        - objectName: signing-private-key
          key: signing-private-key
        - objectName: ldap-bind-password
          key: ldap-bind-password